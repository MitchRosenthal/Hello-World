/**
 * Row type for the `items` table.
 * Table name: items
 * Schema: id (uuid), title (text), created_at (timestamptz)
 */
export type Item = {
  id: string;
  title: string;
  created_at: string;
};
